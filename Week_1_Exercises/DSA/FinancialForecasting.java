package Week_1_Exercises.DSA;

public class FinancialForecasting {
    int count = 0;
    int predictedValue = 0;
    int averageDifference = 0;
    // Change this array according to the values
    int[] pastValues = { 1, 2, 3, 4, 5, 6, 6, 6 };

    public int forecast(int lastSum, int count){
        if(count == pastValues.length){
            return 
        }
        forecast()
    }
}
